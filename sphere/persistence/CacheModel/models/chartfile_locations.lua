local chartfile_locations = {}

chartfile_locations.table_name = "chartfile_locations"

chartfile_locations.types = {
	is_relative = "boolean",
	is_internal = "boolean",
}

chartfile_locations.relations = {}

return chartfile_locations
